/**
 * Italian translation of DSH's UI namespaces.
 *
 * Each entry mirrors the key set of the corresponding `zh` source dictionary.
 * Registered through `ctx.locale.register(ns, 'it', dict)` so DSH's fallback
 * chain returns the Italian text when `it` is the active locale.
 *
 * @module @huanlin/dsh-plugin-better-locale/client/dictionaries/it
 */
/** it dictionaries keyed by DSH namespace. */
export declare const dicts: Record<string, Record<string, string>>;
