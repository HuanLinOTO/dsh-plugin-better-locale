/**
 * Russian (ru) dictionary for the DSH web UI namespaces.
 *
 * Translated from the Simplified-Chinese (zh) source-of-truth dictionaries
 * shipped by the DSH packages listed in the better-locale plugin manifest.
 * Placeholders like `{name}` are preserved verbatim; technical terms (Git,
 * SSH, HTTP, URL, Markdown, PDF, VS Code, Tab, etc.) are kept in English
 * where idiomatic in Russian product UI.
 *
 * Tone: formal «Вы».
 */
export declare const dicts: Record<string, Record<string, string>>;
