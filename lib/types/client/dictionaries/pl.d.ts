/**
 * Polish translations for all DSH built-in i18n namespaces.
 * Generated from the zh source dictionaries in the DSH source tree.
 *
 * Translation conventions:
 * - Formal register (Pan/Pani/Państwo) for user-facing copy.
 * - Polish diacritics used throughout (ą, ć, ę, ł, ń, ó, ś, ź, ż).
 * - {placeholder} patterns kept verbatim (interpolation runs after lookup).
 * - Technical terms left in English where idiomatic (Git, SSH, HTTP, URL,
 *   Markdown, PDF, VS Code, Token, LLM, Cordis, Client, Host).
 * - Date formats use Polish day-first order ({d}.{m}.{y}).
 */
export declare const dicts: Record<string, Record<string, string>>;
