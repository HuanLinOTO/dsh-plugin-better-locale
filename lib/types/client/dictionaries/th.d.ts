/**
 * Thai translations for all DSH built-in i18n namespaces.
 * Generated from the zh source dictionaries in the DSH source tree.
 *
 * Translation conventions:
 * - Technical terms stay in English where appropriate (Git, SSH, HTTP, URL,
 *   Markdown, PDF, VS Code, API Key, Base URL, Provider ID, token, etc.).
 * - Placeholders keep {name} verbatim (interpolation runs after lookup).
 * - Formal Thai appropriate for software UI.
 * - Terminology is kept consistent across namespaces.
 */
export declare const dicts: Record<string, Record<string, string>>;
